<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#000"
                android:padding="2dp">

    <!--This xml is used to create a custom snack bar that displays an image message and button-->
    <ImageView android:layout_width="40dp"
               android:layout_height="40dp"
               android:id="@+id/snackImage01"
               android:layout_alignParentStart="true"
               android:layout_marginLeft="2dp"
               android:layout_marginRight="2dp"
               android:src="@drawable/rightblue"/>

    <TextView android:layout_width="wrap_content"
              android:layout_height="30dp"
              android:id="@+id/snackMessage"
              android:gravity="center_vertical"
              android:layout_marginLeft="10dp"
              android:layout_marginRight="2dp"
              android:layout_toRightOf="@id/snackImage01"
              android:fontFamily="sans-serif-condensed-medium"
              android:text="Hola"
              android:textColor="#fff"/>

    <Button android:layout_width="wrap_content"
            android:layout_height="20dp"
            android:id="@+id/snackButton"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@color/trans_parent"
            android:fontFamily="sans-serif-condensed-medium"
            android:gravity="center_vertical|center_horizontal"
            android:text="OK"
            android:textColor="@color/red"/>
</RelativeLayout>